class Book {
  constructor(title, author, genre, publisher, published_date, format, isbn) {
    this.title = title;
    this.author = author;
    this.genre = genre;
    this.publisher = publisher;
    this.published_date = published_date;
    this.format = format;
    this.isbn = isbn;
  }
}

export default Book;